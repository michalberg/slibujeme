<%= form_for @material do |f| -%>
  <div id="material-form-documents">
      
  </div>
  <div id="material-form-metadata">
      <%= f.label :election_id %>
      <%= f.collection_select :election_id, Election.all, 'id', 'title' %>
      
      <%= f.label :municipality %>
      <%= f.select :municipality, { "Brno (okr. Brno-město)" => "Brno (okr. Brno-město)", "Praha (okr. Praha)" => "Praha (okr. Praha)" } %>
      
      <%= f.label :party %>
      <%= f.select :party, {"ODS" => "ODS", "TOP09" => "TOP09"} %>
      
      <%= f.label :topics %>
      <%= f.text_area :topics %>
      
      <%= f.label :polititians %>
      <%= f.text_area :polititians %>
      
      <%= f.label :comment %>
      <%= f.text_area :comment %>
      
      <%= f.label :contact %>
      <%= f.text_field :contact %>
      
      <div class="controls">
          <%= f.submit "Vložit", class: "btn btn-primary" %>
      </div>
  </div>
<% end -%>