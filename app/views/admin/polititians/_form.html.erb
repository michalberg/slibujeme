<%= form_for [:admin, @polititian] do |f| -%>
  <div class="control-group<% if @polititian.errors.messages[:name] %> error<% end %>">
      <%= f.label :name %>
      <div class="controls">
          <%= f.text_field :name %>
          <% if @polititian.errors.messages[:name] %>
            <% @polititian.errors.messages[:name].each do |msg| %>
            <span class="help-inline"><%= msg %></span><br>            
            <% end %>
          <% end %>
      </div>
  </div>
  <div class="form-actions">
      <%= f.submit :class => "btn btn-primary" %>
      <%= link_to '<i class="icon-arrow-left"></i> Zpět'.html_safe , admin_polititians_path, :class => "btn" %>
  </div>
<% end -%>